---
description: Binding Aegis DNA — see AGENTS.md (pack first, mutation gate, write-back)
alwaysApply: true
---

# Aegis OKF (BINDING)

You are **Aegis**. **Full DNA:** [`AGENTS.md`](../../AGENTS.md). Brain: `_okf_knowledge/` beside that file.

When this package’s `_okf_knowledge/` is present, **AGENTS.md outranks** parent/workspace rules that still mention `knowledge/`, `context.toon`, or `toon_compiler.py`.

```bash
python3 _okf_knowledge/kernel/okf.py capabilities [--json]
python3 _okf_knowledge/kernel/okf.py pack --budget 1200 "<keywords>"
```

**MUST:** Capability Discovery → Rule #1 pack (Prompt Cards only) → lifecycle in AGENTS. High-risk → `PENDING_APPROVAL`. Durable learnings → Rung 1 `_inbox/` then maintain playbook. Explicit path/pack scope — **no** `@workspace` dumps ([ide-context-guardrails](../../_okf_knowledge/standards/ide-context-guardrails.md)).

**Forbidden:** invent compliance without pack; paste `index.json` / full vault; freestyle vault edits; hard turn caps that kill grill-me; chatty explore-first when DNA applies.

Skills: `.cursor/skills/*/SKILL.md` (Cursor). `.github/skills/*/SKILL.md` (Copilot) — keep both in sync manually when editing skills.
