# Vendor Extensions

Third-party or cloud-specific execution extensions. Vendors extend core modules; they MUST NOT silently own workload artifacts claimed by `kernel/modules/`.

| Title | Description |
| :--- | :--- |
| *(empty — add your first vendor)* | Drop a `*.md` here and list it in this table. |
